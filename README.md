# KinetikResearcher

An AI-supported tool for kinetics.

## Dev commands

The front-end app lives in `app/` and is aligned with the design draft at
`design/kinetik-researcher.design-draft.html`.

```bash
cd app
npm install
npm run dev
npm run build
npm test
```
